require "api/fedex/version"

module Fedex
  class Rates
    def self.get(credentials, quote_params)
      puts "#################"
      puts credentials
      puts quote_params
      puts "#################"
    end
  end

end
